> 文件权限字符

```
    示例：
    drw-r-xr-x 6 git git 

    第一个字符代表文件（-）、目录（d），链接（l）
    其余字符每3个一组（rwx），读（r）、写（w）、执行（x）
    
    第一组rw-： 文件所有者的权限是读、写，但是不能执行
    第二组r-x： 权限是读、执行，但是不能写
    第三组r-x： 权限是读、执行，但是不能写
    
    也可用数字表示为：r=4，w=2，x=1 因此 rwx=4+2+1=7
```
